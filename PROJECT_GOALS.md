# OpenCleaner-skills 重构目标与验收

> 状态：1.0.0 技术与许可验收通过（macOS/Windows 真实 runner 已验证）
> 用途：记录代码重构、产品边界、安全要求和验收标准。

## 重要许可说明

版本 1.0.0 已采用 PolyForm Noncommercial License 1.0.0。非商业使用遵循根 `LICENSE`；商业使用必须事先取得 JayHome137 的单独书面许可，联系入口记录在 `COMMERCIAL_LICENSE.md`。

第三方 MIT 内容继续保留原版权声明和 MIT 权利，不重新声明为自有代码。仓库历史中已经按 MIT 发布的版本继续保有原授权，新许可证不追溯撤销既有权利。

## 当前实施状态

| 阶段 | 状态 | 本地证据 |
| --- | --- | --- |
| 阶段 0：基线与来源 | 本地通过 | `docs/BASELINE.md`、`docs/PROVENANCE.md`、三份 schema。 |
| 阶段 1：安全内核 | 本地通过 | 显式 Dry Run、受控 session plan、Trash 后复核、操作日志和空间变化均有测试。 |
| 阶段 2：扫描与规则 | 本地通过 | 有限并发、取消、稳定排序、规则风险、非目标范围及性能基线均已接入。 |
| 阶段 3：报告接入 | 本地通过 | 保留原阅读顺序和颜色语义；action ID、操作历史及桌面/移动布局已验证。 |
| 阶段 4：来源与许可 | 本地通过 | 主体代码和报告均已重写；PolyForm、Required Notice、商业入口和第三方 MIT 边界已落地。 |

逐项证据和未完成边界见 `docs/ACCEPTANCE_MATRIX.md`。

## 项目目标

把 OpenCleaner-skills 重构为一个独立、跨平台、面向 AI Agent 的存储分析与受控处置 Skill：保留当前版本的中文可视化报告和对陌生目录的语义分析能力，同时建立确定性的扫描、策略、执行和审计内核。

项目不以复刻 Mole 或原参考仓库为目标。重构后应拥有独立的模块划分、数据模型、规则体系、安全策略、测试体系和实现代码。

## 不可变边界

| 边界 | 目标要求 |
| --- | --- |
| 产品形态 | 继续以 Codex/Agent Skill 形式交付，不改造成终端清理套件或常驻应用。 |
| 用户界面 | 保留当前 HTML 报告的主要视觉设计、信息层级和阅读顺序。 |
| Agent 价值 | 保留对 UUID 容器、应用数据、开发缓存、离线内容和系统数据的语义解释。 |
| 平台 | 继续支持 macOS 和 Windows；Linux 暂不进入目标范围。 |
| 安全默认值 | 扫描只读；处置默认可预览、可拒绝、可审计、可恢复。 |
| 实现独立性 | 不复制 Mole 或原参考仓库的函数、模块结构、规则文本或代码表达。 |

## 范围

本轮重构包含：

- 存储空间扫描、目录大小统计和扫描覆盖率说明。
- 确定性规则分级与 Agent 补充分析。
- Top 5、绿黄红三级建议、长期建议和 HTML 报告。
- Dry Run、移到废纸篓、打开文件管理器、操作历史和清理后复核。
- macOS 与 Windows 的路径、权限和回收站差异处理。
- 安全策略、规则、扫描器、执行器和报告之间的稳定数据契约。

本轮重构不包含：

- Mole 式实时 CPU、内存、温度和网络监控。
- 系统服务优化、数据库重建、网络刷新或后台维护任务。
- 完整应用卸载器、启动项清理器或软件包管理器。
- 定时任务、常驻服务、菜单栏程序或远程管理能力。
- 未经用户明确授权的管理员权限操作。

## 保留双方优势

| 来源经验 | 保留内容 | 新项目中的实现方式 |
| --- | --- | --- |
| 当前项目 | 中文 HTML 报告、三级风险、Agent 解释、跨平台支持 | 保留报告产品体验，重新定义数据契约和生成流程。 |
| 当前项目 | 对陌生目录进行上下文追查 | Agent 只提供解释和建议，不能直接授予删除权限。 |
| Mole | Dry Run、白名单、操作历史、失败即拒绝 | 在独立的策略与执行模块中重新实现。 |
| Mole | 受控扫描、超时、取消、结构化错误 | 建立单次扫描计划和有限并发，不照搬 Go/Shell 实现。 |
| Mole | 删除入口集中、保护目录和运行状态检查 | 建立本项目自己的跨平台文件操作内核。 |
| Mole | 高风险路径和模糊匹配的回归测试 | 使用临时目录、路径语料库和平台 CI 独立编写测试。 |

## 目标运行链路

```text
Skill 触发
  -> 只读扫描器采集事实
  -> 规范化扫描结果
  -> 确定性规则引擎给出基础分级
  -> Agent 补充目录身份、内容画像和处置说明
  -> 策略验证器生成受控操作计划
  -> 保持现有风格的 HTML 报告
  -> 用户预览并确认
  -> 统一文件操作内核执行
  -> 操作日志与目标路径复扫
```

核心原则：Agent 可以降低操作等级、要求人工判断或补充说明，但不能把程序规则不认可的路径提升为可直接删除。

## 目标模块

建议采用职责导向的独立结构，最终文件名可在实现时按现有仓库风格微调：

```text
opencleaner-skills/
├── SKILL.md
├── assets/
│   └── report_template.html
├── references/
│   ├── macos.md
│   └── windows.md
├── rules/
│   ├── common.json
│   ├── macos.json
│   └── windows.json
└── scripts/
    ├── scan.py
    ├── classify.py
    ├── validate_plan.py
    ├── file_ops.py
    ├── build_report.py
    └── server.py
tests/
├── test_scan.py
├── test_rules.py
├── test_plan_validation.py
├── test_file_ops.py
├── test_server.py
└── path_corpus/
```

模块职责必须单向：扫描器不做删除判断，Agent 不执行文件操作，报告不绕过策略验证器，服务端不自行扩展操作权限。

## 核心安全策略

### 扫描策略

- 扫描阶段严格只读，不创建、移动、删除或修改被扫描目录。
- 扫描根必须去重，避免 Home、Library、Caches 等包含关系造成重复遍历。
- 所有外部命令必须有超时、退出状态和结构化错误，不把失败伪装成空目录。
- 扫描支持取消、有限并发和覆盖率说明；权限拒绝项必须进入报告。
- 缓存必须带 schema 版本和失效依据，不仅依赖父目录 mtime。

### 分级策略

- 绿灯必须同时满足：命中确定性规则、目标可再生、排除项已验证、未命中保护策略。
- 黄灯用于存在用户数据、状态不明、目录身份不完整或需要应用内操作的内容。
- 红灯用于应用本体、核心数据、系统资产和不应由本项目直接处置的项目。
- 未知项默认黄灯，不因名称包含 `cache`、`temp` 或文件较旧就自动变绿。
- 每条规则必须记录适用平台、目标模式、非目标范围、恢复方式和风险说明。

### 操作计划

- 报告中的可执行动作必须来自经过校验的版本化操作计划，不能直接信任 Agent 生成的路径。
- 操作计划至少记录规范路径、动作类型、规则 ID、生成时间和生成时的文件身份。
- 执行前重新检查真实路径、文件身份、父目录、符号链接和保护规则，拒绝陈旧计划。
- 一次请求必须先完整验证全部目标，再执行任何一个目标，避免部分验证后产生越界操作。

### 文件操作

- 第一阶段只允许 `open` 和 `trash`；永久删除不作为默认能力。
- `$HOME`、用户配置根、磁盘根、系统根、应用目录根和回收站根永远不能作为整体操作目标。
- `/Applications` 下的内容只能定位打开，不能通过报告服务直接删除。
- 系统路径和需要管理员权限的目标只提供说明，不由本 Skill 自动执行。
- 所有删除入口必须经过同一个文件操作模块，不允许报告服务直接调用 `shutil.rmtree` 或 `os.remove`。
- Trash 不可用时必须失败，不得自动降级为永久删除。
- 每次操作记录时间、动作、规则、路径、结果、失败原因和复核后的空间变化。

### 本地报告服务

- 只绑定 `127.0.0.1` 和随机端口，使用会话随机 token，并校验 Host。
- 限制请求体大小、目标数量和单次操作类型；拒绝未知字段和未知模式。
- 页面确认不是安全授权来源，服务端必须独立完成全部策略验证。
- 服务停止后 token 和操作计划立即失效。

## 报告界面保留要求

以下阅读顺序和主要内容保持不变：

1. 磁盘总览、容量进度条和系统信息。
2. 占用排行 Top 5。
3. 执行建议和优先级。
4. 绿灯、黄灯、红灯三级折叠卡片。
5. 长期优化建议。

允许增加但不能破坏现有布局的内容：

- 扫描覆盖率、失败目录和结果可信度。
- Dry Run 标记、规则来源和操作原因。
- “移到废纸篓”后的实际释放空间。
- 操作历史、失败原因和重新扫描状态。

报告模板重构完成后必须进行桌面与移动宽度截图对比；主要布局、颜色语义、信息顺序和中文表达不能发生无意回退。

## 独立重写要求

- 先定义本项目自己的输入输出契约、错误模型和安全不变量，再编写实现。
- 不从 Mole 或原参考仓库复制函数体、注释、规则文本、测试用例或目录布局。
- 可以参考公开行为和工程经验，但代码命名、控制流、数据结构和测试夹具必须独立设计。
- 对当前仓库逐文件登记来源：保留、重写、删除或仅保留版权声明。
- 只有在某个模块完成独立实现、测试通过并替换旧入口后，才删除旧实现。
- 重构结束时生成 `THIRD_PARTY_NOTICES.md`，保留仍在发行物中的第三方内容及许可证。
- “已经重写”不能自动消除历史代码的许可证义务；许可证结论以最终发行内容和来源清单为准。

## 实施阶段

### 阶段 0：建立基线与来源清单

产物：当前行为基线、报告截图、数据 schema、第三方来源清单、高风险路径测试语料。

验收条件：当前 macOS/Windows 已声明能力被记录；所有现有文件都有来源和处置状态；未修改现有运行逻辑。

### 阶段 1：安全内核与操作计划

产物：规则模型、操作计划 schema、策略验证器、统一文件操作模块、Dry Run、操作日志。

验收条件：Agent 生成的任意路径都不能绕过策略；主目录、应用目录、系统目录、符号链接和陈旧计划测试全部失败关闭；真实处置仅允许 Trash。

### 阶段 2：扫描器与规则引擎

产物：不重叠扫描计划、有限并发、取消、结构化错误、macOS/Windows 基础规则。

验收条件：同一根不重复遍历；超时和权限错误可见；规则输出可在无 Agent 环境下稳定复现；扫描性能有基线对比。

### 阶段 3：Agent 分析与报告接入

产物：Agent 分析契约、计划验证接入、保留现有视觉的报告、清理后复扫。

验收条件：Agent 只能补充说明或降低权限；报告中的每个按钮都能追溯到规则和操作计划；界面截图对比通过。

### 阶段 4：旧实现退出与许可整理

产物：旧主体代码移除、完整测试矩阵、第三方声明、最终非商业/商业双重许可文本。

验收条件：发行内容不再依赖原参考仓库或 Mole 的主体实现；第三方内容均有准确声明；许可证之间无冲突；README 与实际行为一致。

## 完工标准

- 项目仍可作为 Skill 被正常发现和调用。
- macOS 与 Windows 都能完成扫描、报告、Dry Run 和 Trash 流程验证。
- 报告保留现有界面结构，并能显示扫描可信度、规则来源和实际操作结果。
- 所有破坏性入口统一经过策略验证和文件操作内核。
- Agent 无法通过 analysis JSON 单独授权删除。
- 不存在对 `$HOME`、磁盘根、系统根或 `/Applications` 的整体删除路径。
- 安全测试覆盖符号链接、路径穿越、大小写、陈旧计划、并发请求和 Trash 失败。
- 操作历史能回答“何时、因何规则、对什么路径、执行了什么、结果如何”。
- 当前第三方主体实现完成替换，保留内容具有明确来源和许可证声明。
- 最终许可证准确表达非商业使用与商业授权要求，不再错误标记为 MIT。

## 实施前决策

以下项目在进入对应阶段前必须确定：

| 决策 | 暂定建议 | 最晚确定阶段 |
| --- | --- | --- |
| 新原创代码许可证 | PolyForm Noncommercial 1.0.0 + 单独书面商业许可 | 已确定，1.0.0 起生效 |
| 永久删除 | v1 不提供；只允许 Trash | 阶段 1 |
| 管理员权限 | v1 不执行，只在报告中说明 | 阶段 1 |
| Windows 深度 | 保留当前功能并补真实桌面验证，不追求与 macOS 系统目录完全对称 | 阶段 2 |
| 规则格式 | 版本化 JSON，代码只负责验证与执行 | 阶段 1 |

## 下一步

1.0.0 技术、许可和双平台验收均已完成。实现提交 `ddfc6c4e695579b431a022a43343fdb8427ad6f5` 的 [macOS workflow](https://github.com/JayHome137/OpenCleaner-skills/actions/runs/32548944726) 与 [Windows workflow](https://github.com/JayHome137/OpenCleaner-skills/actions/runs/32548944701) 均成功；后续商业合同仍需按适用司法辖区进行专业法律审阅。
